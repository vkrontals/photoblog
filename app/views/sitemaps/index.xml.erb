<?xml version="1.0" encoding="UTF-8"?>
<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  <sitemap>
    <loc><%= post_sitemap_url(format: :xml) %></loc>
    <lastmod><%= Post.last.created_at.to_date.strftime('%Y-%m-%d') %></lastmod>
  </sitemap>

  <sitemap>
    <loc><%= page_sitemap_url(format: :xml) %></loc>
    <lastmod>2015-11-17</lastmod>
  </sitemap>

  <sitemap>
    <loc><%= image_sitemap_url(format: :xml) %></loc>
    <lastmod><%= Image.last.created_at.to_date.strftime('%Y-%m-%d')  %></lastmod>
  </sitemap>

</sitemapindex>
